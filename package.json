{
  "name": "pythonbee",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "dependencies": {
    "@types/node": "^10.12.12",
    "@types/socket.io-client": "^1.4.32",
    "docker-python-sandbox-mac": "0.0.1",
    "express": "^4.16.4",
    "http": "^0.0.0",
    "path": "^0.12.7",
    "socket.io": "^2.2.0",
    "typescript": "^3.2.2"
  },
  "devDependencies": {},
  "scripts": {
    "start": "tsc server/ts/* --outDir server/js --lib es6,dom && tsc client/ts/* --outDir client/js --lib es6,dom && node server/js/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc-server": "tsc --project tsconfig.server.json",
    "tsc-client": "tsc --project tsconfig.client.json",
    "rm-build": "rm -rf build/server/** && rm -rf build/client/**"
  },
  "author": "",
  "license": "ISC"
}
